<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- 圆角大小 -->
    <corners android:radius="3dp" />

    <!-- 只保留边框，内部透明（删除 solid 标签即可） -->
    <stroke
        android:width="1dp"
        android:color="@color/red_cherry" />

</shape>