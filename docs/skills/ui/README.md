# UI Skills

## Purpose
- Collect all UI generation and refactor skills.

## Buckets
- `forms/`: data-entry and workflow forms.
- `feed/`: list/card/timeline pages (reserved).
- `detail/`: detail and profile pages (reserved).
- `navigation/`: app bars, tabs, bottom nav (reserved).
- `theme/`: color/typography/system tokens (reserved).
